# Hypotheses

One file per promoted hypothesis (`HYP-XXXX.md`). Complete its mechanism, exact
change, primary metric, kill test, required controls, and data scope before
registering an experiment. Use `python tools/research_admin.py new-hypothesis`.
