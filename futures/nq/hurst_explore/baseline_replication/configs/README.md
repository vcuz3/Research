# Frozen Baseline Configs

Store machine-readable JSON, YAML, or TOML configs here. A config should identify
the strategy parameters, execution profile, costs, sizing, metric definitions,
data scope/fingerprint, and schema version. Do not mutate a config already used
by a completed run; create a new version.
