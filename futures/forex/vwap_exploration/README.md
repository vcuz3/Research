# Backtesting Project Template

Copy this directory to a new project location, replace every `<placeholder>`, and
complete paper intake before testing strategy improvements. Do not edit this
template with project-specific findings.

The structure is shared by Claude Code and Codex through plain Markdown, CSV,
JSON, code, and immutable artifacts; no MCP database is required.
