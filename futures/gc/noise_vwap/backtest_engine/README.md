# GC Backtest Engine

Execution/accounting is `core/engine.py` + `core/metrics.py`, verbatim copies of the audited NQ engine (`futures/nq/noise_vwap`). Do not diverge; port fixes from NQ and keep parity.
