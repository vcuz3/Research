# Validation

EXP-0001 is the exact requested baseline plus cost sensitivity. A positive
result remains exploratory until it passes a path-preserving return-shuffle null,
neighbouring parameter checks, era stability, and a future-only shadow sample.
The four requested correlated FX pairs are sibling-market checks, not four fully
independent replications.
