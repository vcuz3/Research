# exploration_4 instructions

Follow the workspace research rules and `PROJECT_PLAN.md`. The baseline parameters
are frozen: do not sweep or tune them. Reuse the audited fill simulators in
`../exploration_1/`. Keep the 2024+ holdout sealed until the final report.

Run the project with:

```powershell
python -u forex/exploration_4/_run_baseline.py
```

Then execute `notebooks/trade_visualizations.ipynb` from the workspace root.
